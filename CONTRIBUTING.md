# Contributing to OpsNexus Dashboard

Keep UI changes aligned with the versioned backend and API contract. Describe affected operator workflows, authentication/token handling, accessibility, responsive behavior, and screenshots where useful.

Run npm ci, npm run lint, and npm run build before review. Do not commit .env.local, dist, node_modules, tokens, or production data. VITE_ values are browser-visible and must never contain secrets.
